정밀 측정이 끝났습니다.

## 최종 확정: 피봇 admission 룩어헤드

| | 표본 40개 | **표본 200개** |
|---|---|---|
| 룩어헤드 발생 시점 | 3개 (7.5%) | **31개 (15.5%)** |
| 최대 선행 | 86봉 (7.2시간) | 47봉 (3.9시간) |

**전체 시각의 약 15%에서, 아직 확정될 수 없는 피봇을 최대 수 시간 앞서 사용**하고 있습니다. 발생률은 첫 표본(7.5%)보다 오히려 높게 나왔습니다.

주목할 점: 예시가 **전부 `'low'` 피봇**입니다. 저점 피봇이 조기 admit되면 `is_bullish`가 상승 파동으로 뒤집히고, fade 전략은 **숏**으로 갑니다 — 즉 "바닥이 이미 찍혔다"는 미래 정보로 방향을 정하는 구조입니다.

---

## 지금까지 확정된 인과 사슬

```
피봇 admission 가드가 P+n을 쓰지만 실제 확정은 i+n (i > P)
  → 시각의 약 15%에서 최대 수 시간 앞선 피봇 사용
  → a_p2(파동 시작)가 조기 확정됨
  → div_rsi_gap 계산 구간이 리셋되어 ≈ 0
  → "div_rsi_gap ≈ 0" = "반전이 이미 일어났다"는 미래 정보
  → fade 진입 타이밍이 비정상적으로 정확 (승률 70.7%, t≈19)
  → 절제 시 승률 48.6%로 붕괴 (엣지 전부가 이 피처에 집중)
```

`div_rsi_gap`은 **누수의 원인이 아니라 가장 날카로운 전달 경로**입니다. 원인은 피봇 엔진이고, 따라서 **v9·v9b·v9c 전 캐시와 지금까지의 모든 학습 결과가 무효**입니다.

---

## 대기 중

수정 진행 승인을 기다리고 있습니다. 확정해주시면:

1. `algorithm.find_dynamic_pivots` — 피봇별 실제 확정 인덱스 반환 (역순 스캔이라 인덱스 환산 주의 필요)
2. `prep_features.py` — 가드를 `confirm_index <= idx_5m`으로 교체
3. admission 테스트로 **위반 0건** 검증
4. 캐시 전체 재생성 → 재학습

**성능은 크게 떨어질 것으로 예상되며 그게 정상입니다.** 현재 지표(sel 1.41, 승률 70.7%)는 실현 불가능한 값입니다.

앞서 말씀하신 3개 항목 중 **2번(`div_rsi_gap` 절제)은 이미 답이 나왔고**(엣지 전체가 여기 집중), **3번(평가 수수료)은 측정 완료**(sel 1.405→1.252, 문제의 주원인 아님), **1번(피봇 파라미터 재탐색)은 이 수정 이후에 해야 의미가 있습니다**.
