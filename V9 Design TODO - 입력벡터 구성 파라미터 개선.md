# V9 Design TODO — 입력벡터(관측) 정규화 상한 개선

상태: **분석 완료, 미적용.** 코드 변경 없음. 2026-07-23~24 대화에서 나온 분석을 문서화.
(관측 22번칸 `liq_dist`는 이미 별도로 수정·적용됨 — 이 문서에서는 제외, `design_spec.md` 6장 07-24 항목 참고.)

## 1. 배경

`env.py`의 `NORM` 딕셔너리(관측 정규화 상수)는 대부분 한 번도 실측 검증 없이 초기값 그대로 쓰이고 있었음. 캐시 분포 리포트(`caches/dist_report_v9b_*.json`)의 p99 값과 대조해보니, 상수 대부분이 **실제 데이터 분포보다 훨씬 느슨하게(크게) 잡혀있어** 관측값이 정규화 범위의 일부만 쓰고 있었음 — 즉 신경망 입력의 표현 해상도가 낭비되고 있었음.

**목표 기준**: 사용자 목표가 "BTC 단일 종목 성공"이므로, 심볼 공통 상수라도 **BTC의 p99 값이 정규화 범위의 약 80%를 쓰도록** 상한을 다시 잡는다 (ETH/SOL은 참고용 학습 데이터일 뿐이라, 그쪽 꼬리가 더 잘리는 트레이드오프는 감수).

## 2. A그룹 — 심볼 간 편차가 커서 BTC 앵커링 시 ETH/SOL 손실이 큰 것들

BTC는 피봇 파라미터가 타이트해(n=3, min_diff=0.75%) 파동 스케일 자체가 ETH/SOL보다 구조적으로 작음. 하나의 전역 상수로 세 심볼을 동시에 잘 맞출 수 없어, BTC 기준으로 맞추면 ETH/SOL은 지금보다 꼬리가 더 잘림 (의도된 트레이드오프).

| 상수 | 현재값 | BTC p99 | 제안값 (BTC p99/0.8) | 비고 |
|---|---|---|---|---|
| `wave_scale_max` | 12.0 (%) | 4.3847 | **5.5** | ETH/SOL은 제안값 기준 각각 p99의 45~47% 지점부터 클립 시작 (지금보다 더 잘림) |
| `atr_pct_max` | 3.0 (atr/close %) | 0.8501 | **1.06** | 3개 상수 중 변화폭이 가장 큼(3.0→1.06, 약 1/3) |
| `duration_log_max` | `log1p(10.0)` (일) | 1.1771일 | `log1p(1.47)` (원시 상한 **1.47일**로 하향, 현재 10일) | `wave_duration_day`의 log1p 안쪽 clip 상한 |
| `div_gap_max` | 5.0 (%) | 1.4905 | **1.86** | `divergence_price_gap_percent`용 |

## 3. B그룹 — ATR로 이미 정규화돼 있어 심볼 간 편차가 거의 없는 것들

타이밍 피처(`ret_5m/15m/1h`)는 원시 %가 아니라 그 시점 ATR%로 나눈 "몇 ATR 움직였나" 단위라, BTC/ETH/SOL이 서로 비슷한 분포를 보임 — A그룹과 달리 트레이드오프 없이 깔끔하게 개선 가능.

| 상수 | 현재값 | BTC p99 | 제안값 (BTC p99/0.8) | 비고 |
|---|---|---|---|---|
| `ret5_atr_clip` | 3.0 | 2.376 | (선택) 2.97 — 이미 p99 사용률 79%로 양호, 변경 우선순위 낮음 |
| `ret15_atr_clip` | 6.0 | 3.704 | **4.6** | |
| `ret1h_atr_clip` | 12.0 | 5.954 | **7.4** | 시야가 길어질수록 기존 상한이 더 느슨했음 |

`vol_ratio_log_max`(`log1p(6.0)`)는 이미 BTC p99 기준 79.2% 사용 중 — **변경 불필요**, 참고로 확인만 해둔 것.

## 4. C그룹 — `upnl_clip` (관측 20번칸, 재설계로 결론남)

**현재값 `(-1.5, 3.0)`의 문제**: leverage=20 가정으로 잡힌 듯한 값인데, 실제 leverage=1 기준 실현손익(/100)은 p1=-0.052~p99=+0.085, 최솟값-0.066~최댓값+0.107 — 상한의 3~5%밖에 못 씀. A/B그룹 중 가장 심각하게 어긋남.

**논의 경과** (하드클립 → log 압축 → tanh → 최종안, 순서대로 기각/채택):
1. `liq_dist`처럼 `log1p` 압축 제안 → 기각. 미실현손익은 청산가처럼 "가까워질수록 결정적으로 중요해지는 경계"가 없어서, 특정 구간에 해상도를 몰아줄 근거가 없음(오히려 큰 손실 구간에서 크기 구분이 흐려지는 역효과).
2. `tanh` 압축 제안 → 기각. tanh도 결국 절댓값이 커지면 해상도가 줄어드는 함수라 log와 같은 문제를 공유함. 수학적으로 "무한 정의역 + 유한 치역 + 전 구간 균일 해상도"는 동시에 성립 불가 — 셋 중 하나는 포기해야 함.
3. **최종안**: 애초에 `margin`/`leverage`를 거치는 "미실현손익" 프레이밍 자체를 버리고, **"진입가로부터 현재가까지의 거리"(순수 가격변동률)로 재정의**. 수식 전개상 `(upnl/margin)/leverage ≈ pos_dir×(close-entry_price)/entry_price`로, leverage가 아예 안 들어가는 값이라 레버리지 실험 때마다 재조정할 필요가 원천적으로 사라짐.

**최종 제안 (클립 없음)**:
```python
# 기존 변수명 "미실현손익(upnl)" → "진입가 대비 현재가 거리"로 개칭
obs[OBS_DIM+1] = pos_dir * (close - entry_price) / entry_price * 10
```
- `×10`은 실측 데이터(leverage=1 기준 p99≈8.5%p, 최댓값≈10.7%p)가 대략 ±1 근방에 오도록 맞춘 값 — 별도 그리드서치 없이 실측 percentile에서 바로 도출.
- 클립을 아예 안 걸기 때문에, 청산 근접 같은 진짜 극단 상황에선 이 값이 ±1을 넘어(이론상 leverage=1 기준 최대 ±10까지) 나올 수 있음 — **의도된 동작**(극단적일수록 값도 더 크게 나와야 정보가 안 사라짐).
- 레버리지 무관 — 어떤 `--leverage` 값으로 실험해도 이 정규화는 재조정 불필요.
- `NORM["upnl_clip"]` 상수 자체가 불필요해짐(제거 대상).

## 5. 적용 순서 — C → B → A, 3단계

7개 상수 전부를 하나씩 격리 검증하기엔 런당 소요 시간이 커서 비현실적. `ent_coef` vs `explore_bonus`처럼 **서로 다른 메커니즘의 경쟁 가설**을 가릴 때만 완전 격리가 필요했고, 이번 NORM 상수들은 전부 "정규화 범위 재보정"이라는 같은 종류의 수정이라 그룹 단위 묶음 적용이 합리적. 다만 **A그룹과 B그룹은 리스크 성격이 달라서**(B=트레이드오프 없음, A=BTC 앵커링 시 ETH/SOL 손실 있음) 이 경계선만은 살려서 3단계로 진행:

1. **C단계 — `upnl_clip` 재설계** (최우선): 계측 불필요·확신도 최고(수식으로 정확히 도출)·현재 가장 심하게 어긋난 상수(사용률 3~5%). 단독 적용 후 재학습으로 검증.
2. **B단계 — `ret15_atr_clip`, `ret1h_atr_clip`** (그다음): 트레이드오프 없는 순수 개선이라 둘을 묶어서 한 번에 적용. 여기서 개선이 확인되면 "정규화 재보정 방향 자체가 맞다"는 근거가 됨.
3. **A단계 — `wave_scale_max`, `atr_pct_max`, `duration_log_max`, `div_gap_max`** (마지막): BTC 앵커링으로 ETH/SOL 꼬리가 더 잘리는 실제 트레이드오프가 있어 별도 단계로 분리 — 결과가 나빠지면 "B는 안전했으니 A그룹, 그중에서도 심볼 간 트레이드오프"까지 원인을 바로 좁힐 수 있음.

각 단계 사이에 재학습 + 베이스라인(`v9_fullctrl_seed0_0722-0702_best`, sel_monthly_log -0.0174) 대비 비교를 거쳐 다음 단계로 넘어갈지 판단.
