# 방향 선택 실험 — 논의 정리 (2026-08-02)

## 배경

피봇 룩어헤드(미래 정보 누출) 버그를 고친 뒤, 파동 반대 방향(fade)으로 거는
현재 매매 규칙의 수익력을 다시 쟀더니 수수료 떼고 나면 거의 0이거나 마이너스로
나왔다. `(n, min_diff)` 파라미터를 바꿔봐도 결과가 달라지지 않았다.

## 질문: 파동/피봇 전처리가 강점 맞나?

**답: 재료(피봇, 피보나치 되돌림, RSI 다이버전스, ADX, 거래량 강도) 자체는
전혀 특별하지 않다.** 리테일 기술적 분석에서 가장 흔하게 쓰는 조합이고,
"실패한 퀀트 로직"이라 부르는 것들 대부분이 같은 재료를 쓴다.

차이가 날 수 있는 지점은 두 곳뿐이다:

1. **고정 임계값이 아니라 RL이 조합을 학습한다.** "RSI 70 넘으면 매도" 식
   하드코딩이 아니라, 18개 피처의 어떤 조합이 좋은 타이밍인지 정책이 직접 찾는다.
2. **검증 습관.** 흔한 실패 사례는 백테스트 숫자(승률 70%, 월 15배 등)가
   좋아 보이면 그대로 실전에 넣는다. 이번에 그 숫자를 의심하고 파고들어
   룩어헤드를 찾아낸 것 자체가 차별점이지, 피봇을 쓴다는 사실이 아니다.

**지금 시점에 "전처리가 강점"이라고 말하는 건 아직 검증 전의 가설이다.**
증명도 기각도 안 된 상태.

## 지금까지 확인된 증거

- 피봇 확정 시점 기준 fade 엣지: 120개 파라미터 조합 전부 t-stat ≈ 0 (BTC는 양수 0개)
- 실제 방향 신호(파동 방향 역추세) 기준 엣지: 대부분 음수
- 1억 스텝 RL 학습 결과: valid/test `sel_monthly_log` 거의 0 또는 음수,
  파라미터를 바꿔도(n=3/0.0075 vs n=10/0.025) 결과 차이 없음

### 그런데 완전히 빈손은 아니다 — 방향 반전 테스트

이미 완료된 두 학습(pA, pB)의 거래 기록에서, **모델이 실제로 고른 진입·청산
시점을 그대로 두고 방향만 반대로 걸어보면** 어떻게 되는지 재계산(재학습 없이
공짜로 확인 가능):

| 구간 | 실제(fade) 손익/승률 | 방향 반전 시 |
|---|---|---|
| pA valid BTC | +25.6 (62.8%) | **−98.2 (15.7%)** |
| pA test ETH | −116.4 (35.4%) | **−195.6 (21.2%)** |
| pB valid BTC | +81.5 (79.8%) | **−179.4 (9.2%)** |
| pB test ETH | +19.5 (68.5%) | **−126.4 (19.1%)** |

8개 구간 전부에서 방향 반전이 훨씬 나쁘다. → 모델이 고른 그 순간들에는
fade가 무작위가 아니라 뭔가 실제 패턴을 잡고 있다는 뜻. 다만 그게
"수수료 이기고 남을 만큼"인지는 아직 모름.

## 다음 계획: 방향을 RL 모델에게 맡기기

현재 구조는 방향이 무조건 "파동 반대"로 고정돼 있고, RL은 진입·청산
**타이밍**만 정한다. 이걸 풀어서 **방향도 모델이 직접 고르게** 한다.

- 행동 공간 변경: `Discrete(3)` {Hold, Enter(fade 자동), Close}
  → `Discrete(4)` {Hold, 롱 진입, 숏 진입, Close}
- 나머지(관측 18칸, 파동 감지, RSI 다이버전스 계산)는 전부 그대로 — 이번 실험은
  전처리를 버리는 게 아니라 그 위에 얹힌 "무조건 반대 방향" 규칙 하나만 떼는 것
- 행동 공간이 바뀌므로 기존 체크포인트 재사용 불가 → 1억 스텝 재학습 필요

### Action Masking 설계

지금은 무포지션/보유중 상태에 따라 불가능한 행동을 물리적으로 막는 방식(`env.py`의
`action_masks()`)을 쓰고 있음. Discrete(4)로 늘어나도 같은 방식을 그대로 확장.

**현재 (Discrete 3: Hold/Enter/Close)**
```python
def action_masks(self):
    if self.pos_dir == 0:
        return [True, True, False]   # Hold, Enter 가능 / Close 불가
    return [True, False, True]       # Hold, Close 가능 / Enter 불가
```

**변경 후 (Discrete 4: Hold/롱진입/숏진입/Close)**
```python
def action_masks(self):
    if self.pos_dir == 0:
        return [True, True, True, False]   # Hold, 롱, 숏 가능 / Close 불가
    return [True, False, False, True]      # Hold, Close 가능 / 롱·숏 불가
```

- 무포지션일 땐 롱·숏 둘 다 열어주고, 보유 중엔 방향 상관없이 둘 다 막는다.
- **보유 중 반대 방향 진입("플립")은 계속 금지.** 지금 설계 원칙("방향을 바꾸려면
  Close 후 다음 결정에서 새로 Enter해야 한다")을 그대로 유지 — 포지션 중 반대
  방향을 바로 열어주면 한 스텝에 청산+반대진입을 허용하는 셈이라 리스크 관리가 애매해짐.
- `sb3-contrib`의 `MaskablePPO`는 마스크 리스트 길이만 보고 동작하므로 행동 개수가
  늘어나도 마스킹 인프라 자체는 손댈 필요 없음 — `action_masks()` 함수 하나만 수정.

### 알려진 리스크

2026-07-16에 정확히 같은 시도(자유 방향 선택)를 했다가 **"검증 4회 연속
거래 0건" 붕괴**로 실패한 이력이 있음. 당시엔 아직 피처에 룩어헤드가 있던
시절이었고, 이후(07-22) `explore_bonus`를 낮춰서 비슷한 콜드스타트 문제를
한 번 풀어낸 경험이 있어 지금 다시 시도하면 결과가 다를 수 있지만,
**또 실패할 가능성도 실제로 있다.**

## 판단 기준

- 성공: 방향 자유 선택 학습에서 test `sel_monthly_log`가 뚜렷한 양수로 나오면
  → 전처리(파동 피처)가 진짜 신호를 담고 있다는 첫 증거
- 실패(거래 0건 붕괴 또는 test 여전히 0/음수): 전처리 자체를 의심할 근거가 쌓임
  → 그다음엔 입력벡터를 파동 기반이 아닌 다른 방식으로 다시 설계하는 것도 고려

## 실행 결과 (2026-08-02)

`env.py`를 Discrete(4){Hold, 롱진입, 숏진입, Close}로 바꿔 BTC+ETH 1억 스텝씩
두 파라미터 세트로 실학습(`_pA`=운영값 run `0802-0234`, `_pB`=재탐색값 run
`0802-0235`) 완료.

**거래 0건 붕괴는 없었다.** 07-16 실패 원인(진입=확실한 비용, Hold=항상 0)은
explore_bonus + action masking으로 여전히 잘 막혔다.

**대신 다른 방식으로 무너졌다** — 방향을 자유롭게 줬더니 모델이 신호 기반
판단이 아니라 그냥 "롱 쪽으로 몰빵"하는 쪽으로 수렴했다.

| 구간 | 롱 | 숏 |
|---|---|---|
| _pA test BTC | 68건 (+21.1) | 12건 (−12.6) |
| _pA test ETH | 149건 (+88.4) | 70건 (−61.8) |
| _pB test BTC | 255건 (−104.7) | 5건 (+5.5) |
| _pB test ETH | 1080건 (−276.1) | 6건 (+1.8) |

`_pB`는 사실상 숏을 거의 안 걸었다(1086건 중 6건). 학습 구간이 대체로
상승장이었던 탓에, 방향을 열어주니 "레버리지 매수 후 계속 보유"에 가까운
전략으로 수렴했고, test 구간의 하락 국면에서 그대로 손실을 봤다.

- `_pA` test: sel_monthly_log BTC −0.026 / ETH −0.112 — fade 고정 때와 비슷하게
  거의 본전.
- `_pB` test: sel −0.43 / −0.69, 복리 재투자 기준 최대낙폭 BTC 140% / ETH 372%
  — fade 고정보다 명백히 더 나쁨.

**결론: 방향 자유화는 기각.** "모델이 고른 타이밍엔 신호가 있다"(위 방향
반전 테스트)와 "방향을 열어주면 그 신호를 활용해 방향까지 잘 고른다"는 서로
다른 명제였고, 이번 실험은 후자를 기각했다. `env.py`/`train.py`를 fade
고정(Discrete(3)) 상태로 원복함 — 이 문서 자체가 "실패한 실험 기록"으로
남는다. 산출물(`models/v10_direction_free/`, `logs/v10_direction_free/`)은
과거 기록으로 보존.

다음 후보는 위 "판단 기준"의 실패 분기대로, 입력벡터(파동 기반 전처리) 자체를
의심하고 재설계를 검토하는 것.

## 다음 아이디어 (2026-08-02)

방향 편향이 왜 생겼는지(피처에 방향 정보가 애초에 없어서인지, 그냥 학습 구간이
상승장이라 매수가 이득이었을 뿐인지) 아직 구분이 안 된 상태에서 곧장 전처리를
갈아엎는 건 성급할 수 있다. 우선순위대로:

**1. 데이터 좌우 반전 진단 (가장 먼저, 저비용)**

가격 시계열을 상하 반전(상승↔하락이 뒤바뀌도록)한 캐시를 하나 더 만들어
같은 방향 자유화 실험을 반복해본다.
- 반전된 데이터에서도 여전히 "롱 방향"(원래 가격 기준으로는 사실 숏)으로
  쏠린다면 → 피처에 진짜 방향 판단 능력이 없고, 그냥 "매수하면 됨"이라는
  방향 무관 편향을 외웠다는 뜻.
- 반전하면 반대쪽(숏)으로 쏠린다면 → 애초에 방향 판단이 아니라 "이 구간은
  오르는 구간이다"라는 추세 감지만 하고 있었다는 뜻(원본 데이터가 상승장이라
  우연히 롱 편향처럼 보였을 뿐).
- 코드 변경 없이 캐시 하나만 더 만들면 되므로 가장 싸게 원인을 좁힐 수 있다.

**2. 전처리(파동 피처) 자체 재설계 (1번 결과에 따라 결정)**

1번에서 "피처에 방향 정보가 없다"로 결론 나면, 파동/피봇 기반 18칸 벡터를
버리고 다른 종류의 피처(단순 모멘텀, 거래량 프로파일, 다중 시간축 가격
패턴 등)로 교체하는 걸 검토. 규모가 큰 작업이라 1번으로 원인을 좁힌
뒤에만 착수.

**3. RL 파이프라인 자체의 유효성 재점검**

fade 고정 상태에서도 수수료 이후 사실상 본전이고, 방향을 풀어줘도
개선이 없었다. "지금 피처 조합 + PPO"가 수수료를 이길 신호를 못 찾고
있다는 신호일 수 있다. RL 없이 단순 규칙(피봇 확정 직후 정해진 방식으로
진입/청산)과 지금 RL 결과를 나란히 비교해, RL이 단순 규칙 대비 실제로
뭔가 더 얻고 있는지부터 확인하는 것도 방법.

우선순위: 1 → (결과에 따라) 2 또는 3.
