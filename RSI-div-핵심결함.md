핵심 결함 ①: 가격과 RSI를 서로 다른 캔들에서 읽음

rsi2_idx = a_p1["index"] if a_p1["index"] < idx_5m else max(0, idx_5m - 1)
rsi2 = rsis_5m[rsi2_idx]
pr2 = a_p1["price"]

a_p1이 forming 합성 극점일 때 index는 지금 형성 중인 5m 캔들인데, price는 p1_idx+1 ~ idx_5m 구간의 max/min(prep_features.py:260-266)입니다. 즉 가격은 3시간 전 고점, RSI는 방금 봉을 짝지어 비교합니다.

다이버전스는 정의상 같은 봉의 (가격, RSI) 쌍 두 개를 비교하는 건데, 그 쌍이 애초에 깨져 있습니다. 그래서 이런 일이 벌어집니다:

▎ 3시간 전 고점 갱신 (그때 RSI 78, 직전 고점 RSI 72 → 다이버전스 아님, 오히려 강세 확인)
▎ → 지금 40% 되돌림, RSI 52
▎ → 공식: pr2 > pr1 ✓, rsi2(52) < rsi1(72) ✓ → "베어리시 다이버전스, 세기 20포인트"

되돌림이 깊어질수록 자동으로 다이버전스가 성립하는 구조입니다.

데이터로 확인

3개 심볼 전체 캐시(약 980만 행) 기준:

┌──────────────────────────┬───────────────┬───────────────┬───────────────┐
│                          │      BTC      │      ETH      │      SOL      │
├──────────────────────────┼───────────────┼───────────────┼───────────────┤
│ div=1 비율               │ 14.3%         │ 19.1%         │ 19.0%         │
├──────────────────────────┼───────────────┼───────────────┼───────────────┤
│ 방향보정 RSI갭 부호 모순 │ 0.2%          │ 0.1%          │ 0.1%          │
├──────────────────────────┼───────────────┼───────────────┼───────────────┤
│ corr(fib_pos, RSI갭)     │ +0.34 / +0.32 │ +0.41 / +0.36 │ +0.36 / +0.31 │
├──────────────────────────┼───────────────┼───────────────┼───────────────┤
│ div=1 파동 age 중앙값    │ 167분         │ 329분         │ 193분         │
└──────────────────────────┴───────────────┴───────────────┴───────────────┘

- 부호 모순이 0.1~0.3%밖에 안 된다 = rsi2가 사실상 항상 rsi_now(직전 마감봉) 입니다. 확정 피봇 경로는 거의 안 타고 forming 경로가 99.7%+. 즉 위 결함은 예외가 아니라 기본 동작입니다.
- fib_pos(되돌림 깊이)와 "다이버전스 세기"의 상관 +0.31~+0.41. 되돌림이 깊을수록 갭이 커진다는 게 데이터로 그대로 나옵니다. 다이버전스 세기가 아니라 되돌림 진행도를 재고 있다는 증거.

핵심 결함 ②: 정작 다이버전스의 세기는 버려짐

저장되는 건 rsi_previous(= rsi1 절대값)와 divergence_price_gap_percent(가격 갭)뿐입니다. 다이버전스의 본질인 RSI 갭(rsi1 - rsi2)은 어디에도 안 들어갑니다. 관측벡터(env.py:137-142)도 has_div / rsi_adj(=rsi1) / price_gap 3개라, 모델은 RSI가 얼마나 벌어졌는지 알 수 없습니다. rsi_now가 별도로 있어서 우연히 복원 가능한 경우가 대부분이지만, 그건 위 ①번 버그 덕분이지 설계가 아닙니다.

핵심 결함 ③: 필터 전무

- RSI 임계값 없음 — 구버전에 있던 >70 / <30 조건이 사라져서, 과매수/과매도가 아닌 구간에서도 성립합니다 (bearish 중 rsi1>70은 BTC 35%뿐).
- 최소 RSI 갭 없음 — 0.1포인트 차이도 다이버전스 (갭 <1pt가 4~6%).
- 두 극점 간 최소 캔들 거리 없음 (구버전 min_candle_dist=5 소실).
- if a_p3["type"] == a_p1["type"] (:292)는 피봇이 high/low 교대라 항상 참인 no-op 가드입니다.
- 한 번 켜지면 파동이 바뀔 때까지 계속 1 — 이벤트가 아니라 수 시간짜리 상태 플래그입니다.

수정 방향 (제안)

1. 극점 캔들 인덱스를 같이 추적. memo_max_h = highs_5m[...].max() → argmax로 바꿔 극점 봉 인덱스를 얻고, 그 봉의 RSI를 rsi2로 사용. forming 봉이 극점이면 미마감이라 RSI가 없으므로 → 직전 마감봉으로 클램프하되 그 경우는 다이버전스 판정에서 제외(또는 마감 후 확정).
2. rsi_divergence_gap(방향보정 rsi1 - rsi2) 피처 신설 — 가격 갭 대신/과 함께 관측벡터에 투입.
3. 필터 복원: rsi1 과매수/과매도 조건, 최소 RSI 갭(3~5pt), 최소 봉 간격.
4. algorithm.py:96-164의 죽은 코드(calc_fibo_levels, check_rsi_divergence) 정리.